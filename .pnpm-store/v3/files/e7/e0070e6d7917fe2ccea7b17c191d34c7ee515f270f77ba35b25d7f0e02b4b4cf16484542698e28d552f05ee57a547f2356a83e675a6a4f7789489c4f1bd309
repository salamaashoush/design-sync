import type { FieldValues, FormStore, Maybe, ResponseData } from '../types';
/**
 * Updates the dirty state of the form.
 *
 * @param form The store of the form.
 * @param dirty Whether dirty state is true.
 */
export declare function updateFormDirty<TFieldValues extends FieldValues, TResponseData extends ResponseData>(form: FormStore<TFieldValues, TResponseData>, dirty?: Maybe<boolean>): void;
