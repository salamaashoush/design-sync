import { NAMED_COLORS } from './private/named-colors.js';
export function convertNamedColorToHexColor(namedColor) {
    const hexColor = NAMED_COLORS[namedColor.toLowerCase()];
    if (typeof hexColor === 'undefined') {
        return null;
    }
    return hexColor;
}
//# sourceMappingURL=convert-named-color-to-hex-color.js.map