import { OverrideComponentProps } from "@kobalte/utils";
import { JSX } from "solid-js";
import { AsChildProp } from "../polymorphic";
export interface PopperPositionerOptions extends AsChildProp {
    /** The HTML styles attribute (object form only). */
    style?: JSX.CSSProperties;
}
export interface PopperPositionerProps extends OverrideComponentProps<"div", PopperPositionerOptions> {
}
/**
 * The wrapper component that positions the popper content relative to the popper anchor.
 */
export declare function PopperPositioner(props: PopperPositionerProps): JSX.Element;
