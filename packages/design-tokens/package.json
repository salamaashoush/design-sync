{
  "name": "@design-sync/design-tokens",
  "version": "0.0.1",
  "description": "Design tokens for Tokenize",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "lint": "oxlint --import-plugin --deny-warnings && oxfmt --check src",
    "lint:fix": "oxlint --import-plugin --deny-warnings --fix && oxfmt src",
    "test:types": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "@vanilla-extract/css": "^1.18.0",
    "@vanilla-extract/recipes": "^0.5.7",
    "@vanilla-extract/sprinkles": "^1.6.5"
  },
  "devDependencies": {
    "@design-sync/tsconfig": "workspace:*",
    "oxfmt": "^0.32.0",
    "oxlint": "^1.47.0",
    "typescript": "^5.9.3"
  }
}
