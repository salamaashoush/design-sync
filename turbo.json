{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "cache": false
      // "persistent": true
    },
    "lint": {},
    "lint:fix": {},
    "test": {
      "outputs": ["coverage/**"]
    },
    "format": {},
    "test:types": {},
    "precommit": {}
  }
}
