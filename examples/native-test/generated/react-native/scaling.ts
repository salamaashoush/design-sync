/* This file is generated automatically by Design Sync. Do not edit this file directly. */

// Auto-generated React Native responsive scaling utilities
// Do not edit manually

import { Dimensions, PixelRatio } from "react-native";

const BASE_WIDTH = 375;

const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get('window');

/**
 * Scale a value proportionally to the screen width
 * @param size - The base size to scale
 * @returns Scaled size based on screen width
 */
export function scale(size: number): number {
  return PixelRatio.roundToNearestPixel((SCREEN_WIDTH / BASE_WIDTH) * size);
}

/**
 * Scale a value with moderation factor (for typography)
 * @param size - The base size to scale
 * @param factor - Moderation factor (0-1), default 0.5
 * @returns Moderately scaled size
 */
export function moderateScale(size: number, factor: number = 0.5): number {
  return PixelRatio.roundToNearestPixel(size + (scale(size) - size) * factor);
}

/**
 * Scale a value vertically (based on screen height)
 * @param size - The base size to scale
 * @returns Vertically scaled size
 */
export function verticalScale(size: number): number {
  const BASE_HEIGHT = 667;
  return PixelRatio.roundToNearestPixel((SCREEN_HEIGHT / BASE_HEIGHT) * size);
}

/**
 * Responsive font size helper
 * @param size - Base font size
 * @returns Responsive font size
 */
export function fontSize(size: number): number {
  return moderateScale(size, 0.25);
}

/**
 * Responsive spacing helper
 * @param size - Base spacing size
 * @returns Responsive spacing
 */
export function spacing(size: number): number {
  return scale(size);
}

/**
 * Screen dimensions
 */
export const screen = {
  width: SCREEN_WIDTH,
  height: SCREEN_HEIGHT,
  isSmall: SCREEN_WIDTH < 375,
  isMedium: SCREEN_WIDTH >= 375 && SCREEN_WIDTH < 414,
  isLarge: SCREEN_WIDTH >= 414,
} as const;

export type ScalingFunction = (size: number) => number;