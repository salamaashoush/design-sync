/* This file is generated automatically by Design Sync. Do not edit this file directly. */

package com.example.theme

import android.os.Build
import androidx.compose.foundation.isSystemInDarkTheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.material3.MaterialTheme
import androidx.compose.runtime.Composable
import androidx.compose.material3.dynamicDarkColorScheme
import androidx.compose.material3.dynamicLightColorScheme
import androidx.compose.ui.platform.LocalContext

private val LightColorScheme = lightColorScheme(
    primary = LightColors.AccentBrandPrimary,
    secondary = LightColors.AccentBrandSecondary,
    tertiary = LightColors.AccentBlue,
    onBackground = LightColors.BackgroundOverlayContextDefault,
    surface = LightColors.BackgroundLayerDefault,
    outline = LightColors.BorderTintOutline
)

private val DarkColorScheme = darkColorScheme(
    primary = DarkColors.AccentBrandPrimary,
    secondary = DarkColors.AccentBrandSecondary,
    tertiary = DarkColors.AccentBlue,
    onBackground = DarkColors.BackgroundOverlayContextDefault,
    surface = DarkColors.BackgroundLayerDefault,
    outline = DarkColors.BorderTintOutline
)

/**
 * App theme composable
 *
 * @param dynamicColor Enable Android 12+ dynamic colors (Material You)
 */
@Composable
fun AppTheme(
    darkTheme: Boolean = isSystemInDarkTheme(),
    dynamicColor: Boolean = true,
    content: @Composable () -> Unit
) {
    val colorScheme = when {
        dynamicColor && Build.VERSION.SDK_INT >= Build.VERSION_CODES.S -> {
            val context = LocalContext.current
            if (darkTheme) dynamicDarkColorScheme(context) else dynamicLightColorScheme(context)
        }
        darkTheme -> DarkColorScheme
        else -> LightColorScheme
    }

    MaterialTheme(
        colorScheme = colorScheme,
        typography = AppTypography.typography,
        shapes = AppShapes.shapes,
        content = content
    )
}