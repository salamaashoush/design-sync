{
  "name": "@design-sync/styled-components-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "oxlint --import-plugin --deny-warnings --ignore-pattern src/tokens && oxfmt --check src",
    "lint:fix": "oxlint --import-plugin --deny-warnings --ignore-pattern src/tokens --fix && oxfmt src",
    "preview": "vite preview",
    "tokens:sync": "design-sync sync"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "styled-components": "^6.3.9"
  },
  "devDependencies": {
    "@design-sync/cli": "workspace:*",
    "@design-sync/styled-components-plugin": "workspace:*",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react-swc": "^4.2.3",
    "oxfmt": "^0.32.0",
    "oxlint": "^1.47.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
