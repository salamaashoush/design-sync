{
  "name": "tokenize",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "turbo run build --scope=@tokenize/figma-plugin",
    "test": "turbo test",
    "lint": "turbo lint",
    "lint:fix": "turbo lint",
    "verify": "turbo verify",
    "precommit": "turbo precommit",
    "postinstall": "husky install",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,scss,md}\""
  },
  "license": "MIT",
  "dependencies": {
    "husky": "8.0.3",
    "prettier": "^2.8.4",
    "turbo": "^1.8.3",
    "typescript": "^4.9.5"
  },
  "packageManager": "pnpm@7.22.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
